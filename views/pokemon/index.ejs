<div class="container">
    <h1 class="text-center">Favorite Pokemons</h1>
  
    <% pokemons.forEach(function(pokemon) { %>
      <div class="well">
        <h2><a href="pokemon/<%= pokemon.id %>"><%= pokemon.name %></a></h2>
      </div>
    <% }); %>
</div>