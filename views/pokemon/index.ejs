<div class="container">
    <% pokemon.forEach((pokemon) => { %>
        <div class="well">
            <h2>
                <a href="/pokemon/<%= pokemon.name %>"><%= pokemon.name%></a>
                <%= pokemon.name %>
            </h2>
        </div>
    <% }) %>
</div>